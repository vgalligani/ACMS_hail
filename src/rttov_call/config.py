import osfrom datetime import datetimedef config_folders(server):     if 'yakaira' in server:         #------------------------------------------------------------------------------        # Define the folder name to store the plots        parent_dir = '/home/vito.galligani/datosmunin3/Work/Studies/HAILCASE_10112018/'        plot_dir   = 'Plots/'                # Within this folder, define the name of a sub-folder according to date        plotpath       = os.path.join(parent_dir,plot_dir,datetime.now().strftime('%d%m%Y'))                # If the latter sub-folder does not exist, create it.        if not os.path.exists(plotpath):            os.makedirs(plotpath)        if not os.path.exists( os.path.join(plotpath, 'RTTOV')):            os.makedirs(os.path.join(plotpath, 'RTTOV'))        if not os.path.exists( os.path.join(plotpath, 'poster')):            os.makedirs(os.path.join(plotpath, 'poster'))        #------------------------------------------------------------------------------                folders = {}        folders['read_out_dir'] = '/home/vito.galligani/datosmunin3/Work/HAILCASE_10112018_datos/RTTOVout/'           elif 'cnrm' in server:        #------------------------------------------------------------------------------        # Define the folder name to store the plots        parent_dir = '/home/galliganiv/Work/HAILCASE_10112018/'        plot_dir   = 'Plots/'                # Within this folder, define the name of a sub-folder according to date        plotpath       = os.path.join(parent_dir,plot_dir,datetime.now().strftime('%d%m%Y'))                # If the latter sub-folder does not exist, create it.        if not os.path.exists(plotpath):            os.makedirs(plotpath)        if not os.path.exists( os.path.join(plotpath, 'RTTOV')):            os.makedirs(os.path.join(plotpath, 'RTTOV'))        #------------------------------------------------------------------------------                folders = {}        folders['read_out_dir'] = '/home/galliganiv/Work/HAILCASE_10112018/RTTOVinout/RTTOVout/'                   elif 'laptop' in server:        #------------------------------------------------------------------------------        # Define the folder name to store the plots        parent_dir = '/Users/vito.galligani/Work/Studies/HAIL_20181110'        plot_dir   = 'Plots/'                # Within this folder, define the name of a sub-folder according to date        plotpath       = os.path.join(parent_dir,plot_dir,datetime.now().strftime('%d%m%Y'))                # If the latter sub-folder does not exist, create it.        if not os.path.exists(plotpath):            os.makedirs(plotpath)        if not os.path.exists( os.path.join(plotpath, 'RTTOV')):            os.makedirs(os.path.join(plotpath, 'RTTOV'))        if not os.path.exists( os.path.join(plotpath, 'poster')):            os.makedirs(os.path.join(plotpath, 'poster'))        #------------------------------------------------------------------------------                folders = {}        folders['read_out_dir'] = '/Users/vito.galligani/Work/Studies/HAIL_20181110/RTTOVout/'                           return plotpath, folders 